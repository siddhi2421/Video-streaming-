<!-- views/welcome.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        /* Full-page background styling */
        body, html {
            height: 100%;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), 
                        url('https://i.pinimg.com/736x/19/8b/2f/198b2f01e73b905772279616eccc7c65.jpg') center center/cover no-repeat;
            color: white;
        }
        .container {
            text-align: center;
            max-width: 600px;
        }
        h1 {
            font-size: 2.5rem;
            font-weight: 700;
        }

        /* Button styling */
        .btn-custom {
            position: relative;
            display: inline-block;
            font-size: 1.2rem;
            padding: 0.8rem 2rem;
            margin: 0.5rem;
            border-radius: 5px;
            background-color: #E50914;
            border: none;
            color: white;
            transition: background-color 0.3s ease;
            cursor: pointer;
            overflow: hidden;
        }

        .btn-custom svg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            fill: none;
            stroke: white;
            stroke-dasharray: 150 480;
            stroke-dashoffset: 150;
            transition: stroke-dashoffset 1s ease-in-out;
        }

        .btn-custom:hover {
            background-color: #000000;
            color: white;
        }

        .btn-custom:hover svg {
            stroke-dashoffset: -480;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome to StreamSphere! Discover, Stream, and Connect. Start Now!</h1>
        <div class="mt-4">
            <a href="/signup">
                <button class="btn btn-custom">
                    <svg width="100%" height="100%" viewBox="0 0 180 60">
                        <polyline points="179,1 179,59 1,59 1,1 179,1" />
                    </svg>
                    <span>Sign Up</span>
                </button>
            </a>
            <a href="/login">
                <button class="btn btn-custom">
                    <svg width="100%" height="100%" viewBox="0 0 180 60">
                        <polyline points="179,1 179,59 1,59 1,1 179,1" />
                    </svg>
                    <span>Log In</span>
                </button>
            </a>
        </div>
    </div>
</body>
</html>
